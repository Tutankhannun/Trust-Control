/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/Trust-Control',
  assetPrefix: '/Trust-Control/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
